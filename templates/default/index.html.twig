{% extends 'base.html.twig' %}

{% block title %}Page d'accueil{% endblock %}

{% block body %}

    <div class="text-center">
        {% if is_granted('ROLE_USER') %}
        <h4>Bonjour, {{ app.user.name }}</h4>
        {% endif %}
    </div>

    {% include "default/_index.html.twig" %}

{% endblock %}
